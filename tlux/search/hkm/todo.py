# Support hash bit mask at build and search time.
# Support arbitrary metadata columns instead of fixed schema.
# Make the indexer take as an argument the metadata columns, numeric value ranges, and number of unique categorical values.
# 
# Pack multiple files into single doc shard.
