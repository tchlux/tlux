#!/bin/sh
set -eu

bin_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "$bin_dir/hkm-python" -c "import sys; sys.argv[0]='hkm-search'; from tlux.search.hkm.search.searcher import main; main()" "$@"
