#!/bin/sh
set -eu

bin_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
if [ "${1:-}" = "--help" ] || [ "${1:-}" = "-h" ]; then
    echo "usage: hkm-tui"
    exit 0
fi
exec "$bin_dir/hkm-python" -c "import sys; sys.argv[0]='hkm-tui'; from tlux.search.hkm.tui import main; main()" "$@"
